const map = new Map(); // Constructor

map.set('key', 'value'); // add a key value
map.set('a', 1);

console.log(map.get(a)) // expected output 1

console.log(map.size); // get the length of the map

map.delete('key'); // delet the establish key

console.log(map.keys); // returns an iterable with the keys of the map

